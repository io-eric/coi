// Test: while loop syntax
component WhileLoopTest {
    mut int count = 0;

    init {
        while (count < 5) {
            count++;
        }
    }

    view {
        <div>Count: {count}</div>
    }
}

app {
    root = WhileLoopTest;
}
