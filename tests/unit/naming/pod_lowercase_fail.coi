// Test: pod type names must start with uppercase
pod user {  // Should fail - lowercase
    string name;
    int age;
}

component Test {
    mut user u;

    view {
        <div>Test</div>
    }
}

app {
    root = Test;
}
