// Test: Pod types cannot contain no-copy types like Canvas, Audio, etc.
pod GameState {
    string name;
    Canvas canvas;  // Should fail - Canvas is a no-copy type
}

component FailTest {
    mut GameState state;

    view {
        <div>Test</div>
    }
}

app {
    root = FailTest;
}
