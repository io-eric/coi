// Test: mut modifier should not be allowed in pod types
pod BadData {
    mut string name;  // Should fail
    int age;
}

component FailTest {
    mut BadData data;

    view {
        <div>Test</div>
    }
}

app {
    root = FailTest;
}
