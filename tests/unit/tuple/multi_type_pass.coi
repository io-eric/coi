// Test: tuple with multiple types
component TupleMultiType {
    def getStats(): (int count, float average, bool valid) {
        return (42, 3.14, true);
    }
    
    def test(): void {
        (int c, float avg, bool ok) = getStats();
    }

    view {
        <div>test</div>
    }
}

app {
    root = TupleMultiType;
}
