// Test: Reference Params must be bound by the parent.
// You cannot instantiate a component with a reference param
// without passing a valid reference variable to it.

component Child(mut int& val) {
    // Reference param (internally a pointer)
    view { <div>{val}</div> }
}

component App {
    view {
        // Error: Missing required param '&val'. 
        // Reference params cannot be default-initialized and must be provided.
        <Child />
    }
}

app {
    root = App;
}
